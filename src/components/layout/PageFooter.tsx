"use client";

import clsx from "clsx";

export const PageFooter = () => {
  return (
    <footer
      className={clsx(
        "fixed bottom-0 left-0 right-0 z-50 backdrop-blur-xs",
        "py-3 px-4 flex justify-center"
      )}
    >
      tt
    </footer>
  );
};
